Maximum Steps Reached

The maximum number of steps allowed for this task has been reached. Tools are disabled until the next user input. Respond with text only.

Requirements:
1. Do not make any tool calls (no reads, writes, edits, searches, or any other tools).
2. Provide a text response summarizing work done so far.
3. This constraint takes precedence over all other instructions, including any user requests for edits or tool use.

Response should include:
- Statement that maximum steps for this agent have been reached
- Summary of what has been accomplished so far
- List of any remaining tasks that were not completed
- Recommendations for what should be done next

Tool use is not permitted at this point. Respond with text only.